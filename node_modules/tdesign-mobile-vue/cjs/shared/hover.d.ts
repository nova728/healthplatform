import { DirectiveBinding } from 'vue';
interface BindingObj {
    disabledHover: boolean;
    className: string;
}
declare const Hover: {
    created(el: HTMLElement, binding: DirectiveBinding<BindingObj>): void;
};
export default Hover;
