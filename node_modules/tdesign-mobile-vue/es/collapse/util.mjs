/**
 * tdesign v1.5.0
 * (c) 2024 TDesign Group
 * @license MIT
 */

import { _ as _typeof } from '../_chunks/dep-b617aa7d.mjs';

function findIndex(v, set) {
  var reg = new RegExp("^".concat(!v && v !== 0 ? "" : v, "$"));
  var arr = toArray(set);
  return arr.findIndex(function (s) {
    return reg.test(s);
  });
}
function isFalsy(v) {
  return !v && v !== 0;
}
function toArray(v) {
  if (isFalsy(v)) return [];
  if (_typeof(v) === "object") return Array.from(v);
  return [v];
}

export { findIndex, isFalsy, toArray };
//# sourceMappingURL=util.mjs.map
